<!DOCTYPE html>
<html>
    <head>
        <title>Error!!</title>
          <link rel="icon" href="/images/weather.png">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            {{title}}
            {{errorMessage}}
        </div>
        {{>footer}}
    </body>
</html>